// As estruturas não precisam ter nenhum campo.

// Como mencionado no capitulo 1, a unit é outra palavra para uma tupla vazia ().

// É por isso que esse tipo de estrura é chamado de unit-like.

// Este tipo de estrutura raramente é usado.
struct Marker;

fn main() {
    let _m = Marker;
}